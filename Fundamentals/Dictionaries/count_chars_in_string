words = input()
dictionary = {}
for word in words:
    if word == ' ':
        continue
    if word not in dictionary:
        dictionary[word] = 0
    dictionary[word] += 1
for (key, value) in dictionary.items():
    print(f'{key} -> {value}')
